---
import { getLangFromUrl, useTranslations, getTranslatedPath } from '@/i18n/utils'
const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
---

<section
  id="projects"
  class="border-container flex w-full items-center justify-center rounded-2xl !border-none text-gray-800 dark:text-gray-100"
>
  <h2 class="text-2xl font-bold md:text-4xl">{t('projects.title')}</h2>
</section>
